# #app:
# debug: FALSE
# JWT_ACCESS_TOKEN: ${JWT_ACCESS_TOKEN}
# JWT_REFRESH_TOKEN: ${JWT_REFRESH_TOKEN}


# smtp:
#   MAIL_PORT: 465
#   MAIL_USE_TLS: FALSE
#   MAIL_USE_SSL: TRUE
#   MAIL_SERVER: 'smtp.gmail.com'
#   MAIL_USERNAME: ${MAIL_USERNAME}
#   MAIL_PASSWORD: ${MAIL_PASSWORD}
#   SMTP_SECRET_KEY: ${SMTP_SECRET_KEY}
#   # MAIL_PASSWORD: ngwk dzex gurv vuur





# Fixed (CORRECT)
MAIL_SERVER: 'smtp.gmail.com'
MAIL_PORT: 465
MAIL_USERNAME: ${MAIL_USERNAME}
MAIL_PASSWORD: ${MAIL_PASSWORD} 
MAIL_USE_TLS: FALSE
MAIL_USE_SSL: TRUE
MAIL_DEFAULT_SENDER: ${MAIL_USERNAME}
# # Keep your app config separate
# app:
#   debug: FALSE
#   JWT_ACCESS_TOKEN: ${JWT_ACCESS_TOKEN}
#   JWT_REFRESH_TOKEN: ${JWT_REFRESH_TOKEN}

# # SMTP secret can stay under smtp if needed for your serializer
# smtp:
#   SMTP_SECRET_KEY: ${SMTP_SECRET_KEY}